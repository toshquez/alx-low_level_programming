#include "main.h"

/**
 * cap-string - converts small letters to uppercase
 * @s: checked
 * Return: s
 */

char *cap_string(char *s)
{
	int i, j, separatorFound;

	separatorFound = 1;
	for (i = 0)
}
